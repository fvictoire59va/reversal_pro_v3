# ============================================================================
# Reversal Detection Pro — Production Environment Variables
# ============================================================================
# Copier ce fichier en .env sur votre serveur ou configurer dans Portainer
# Portainer > Stack > Environment variables
# ============================================================================

# ── Image tag (latest par défaut, ou sha/date spécifique) ──
IMAGE_TAG=latest

# ── Port d'accès à l'application ──
APP_PORT=8080

# ── Database ──
POSTGRES_USER=reversal
POSTGRES_PASSWORD=CHANGEME_strong_password_here
POSTGRES_DB=reversaldb

# ── Paramètres applicatifs ──
AUTO_REFRESH_ENABLED=true
AUTO_REFRESH_INTERVAL_MINUTES=5
AGENT_CYCLE_INTERVAL_MINUTES=1

# ── Hyperliquid (optionnel, mode paper sans ces valeurs) ──
HYPERLIQUID_WALLET_ADDRESS=
HYPERLIQUID_API_SECRET=

# ── Telegram Notifications (optionnel) ──
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_ENABLED=false
